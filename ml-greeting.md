This is a **model preview**. Ask in English to fit, test, or cluster the loaded table (`data`). You will see a ggplot, a metric card, and the R that ran. Switch Claude vs Grok in the sidebar.

R fits a small tidymodels workflow (or a t-test, PCA, k-means) — not a production pipeline. Every supervised fit is scored against a named baseline.

Try:

- Forecast the next 3 months of revenue
- Predict revenue from quantity, channel, and promo
- Will this order use a promo?
- Is the revenue difference by channel real?
- What's in this table?
- Show the ROC curve
